<!-- signature -->
LeapIn
<!-- text -->
<% if (isManager) { %>[<%= companyName || tenantName %>] 请<% if (deadlineText) { %>于 <%= deadlineText %> 前<% } %>完成角色评估：<%= inviteUrl %><% } else { %>[<%= companyName || tenantName %>] 请<% if (deadlineText) { %>于 <%= deadlineText %> 前<% } %>完成角色访谈：<%= inviteUrl %><% } %>
